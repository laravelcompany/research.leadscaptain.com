# Autonomous Lead Research Agent
Go + React lead research platform.
## Quick start
cp .env.example .env
make build
make dev
# Frontend
cd frontend && npm install && npm run dev
## Docker
docker compose up --build
